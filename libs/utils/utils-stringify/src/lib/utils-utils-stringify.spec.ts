import { utilsUtilsStringify } from './utils-utils-stringify';

describe('utilsUtilsStringify', () => {
  it('should work', () => {
    expect(utilsUtilsStringify()).toEqual('utils-utils-stringify');
  });
});
